import 'package:flutter/material.dart';

class BehavioralAssessmentScreen extends StatelessWidget {
  const BehavioralAssessmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Behavioral Assessment")),
      body: const Center(child: Text("Behavior Analysis Screen")),
    );
  }
}