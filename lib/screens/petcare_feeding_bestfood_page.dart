import 'package:flutter/material.dart';

class PetcareFeedingBestfoodPage extends StatelessWidget {
  const PetcareFeedingBestfoodPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Best Food")),
      body: const Center(child: Text("Best Food Page")),
    );
  }
}